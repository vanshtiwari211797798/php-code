<?php
    // super global variable is access anywhere in the code it decalreing using $GLOBALS keyword here is a example of the super global variable
    $GLOBALS['a']=10;
    // $_SERVER['PHP_SELF'];
    // $_REQUEST['name'];
    // $_POST['name'];
    // $_GET['name'];
    // $_FILES['name'];
    // $_ENV['name'];
    // $_COOKIE['name'];
    // $_SESSION['name'];

    
    // these are the super global variable is used in the php for the different purpose

    // function demo(){
    //     echo $GLOBALS['a']; // use global variable in the function using key and value pair
    //     echo "<br>";
    //     $GLOBALS['b']=900000; // declare the global variable in the function
    // }
    // demo();
    // echo $GLOBALS['b']; //access the global variable outside the function
    
    

    // $_SERVER super global variable it content the information about the server, header, path, script location etc

?>  