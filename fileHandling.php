<?php
// read the file
    // readfile("hello.txt");

    // open the file and read or write
    // $data= fopen("hello.txt",'r');
    // echo fread($data,filesize("hello.txt")); fgets($data) -> first line, fgetc($data) -> first character// 100 or filesize is the length of the characters which is available in the file
    // echo fgets($data); -> first line of the code get
    // echo fgetc($data); -> first character get
    // while(!feof($data)){
    //     echo fgetc($data);
    // }
    // fclose($data); -> close the file

    // write file

    // $res = fopen("hello2.txt",'w');
    // $name = 'vansh tiwari';
    // echo fwrite($res,$name); -> writing the file
    // fclose($res); -> when file is write then close the file
    
?>